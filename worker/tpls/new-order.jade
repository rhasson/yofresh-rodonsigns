div
  h2 New Order Confirmation
  p.
    #{user.first} thank you for placing an order with YoFresh@RodonSigns.
    Your order confirmation number is: #{confirmation_number}

  hr

  h3 Order Summary:
  table
    thead
      tr
        th Product name
        th Quantity
        th Price $
    tbody
      each item in items
        tr
          if (('selected_flavors' in item) && item.selected_flavors.length > 0)
            td= item.name + ' (' + item.selected_flavors.toString() + ')'
          else
            td= item.name
          td= item.quantity
          td= item.total

  p Subtotal: $#{subtotal}
  p Shipping: $#{shipping}
  p Tax: $#{tax}
  p Total payment: $#{total}

  hr
  
  p If you have any questions don't hesitate to email or call us any time.
  p Email: 
    a(mailto: 'sales@rodonsigns.com') sales@rodonsigns.com
  p Phone: 215-885-5358